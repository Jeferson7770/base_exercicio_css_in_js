import { Form, HeroContent, HeroTitle } from './styles'

const Hero = () => (
  <Form>
    <HeroContent>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </HeroContent>
  </Form>
)

export default Hero
